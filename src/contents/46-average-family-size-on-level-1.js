import React from 'react'
import * as H from '../components/h'

export default () => (
  <p>
    On Level 1, in most places, families have on average more than five
    children, and on average one child dies. See: Graph: Family size by income
  </p>
)
